import MyButton from './MyButton'
import { MemoryRouter } from 'react-router-dom'

export default {
    title: 'UI/MyButton',
    component: MyButton
}

export const BlackButton = {
    args: {
        children: 'Button'
    }
}
